#include <iostream>
#include <string>

// Функция для вывода задания
void display_task(int task_number, const std::string& task_description, const std::string& formula) {
    // \033[2J\033[1;1H - Очищает экран и перемещает курсор в верхний левый угол
    std::cout << "\033[2J\033[1;1H"; 

    // Вывод заголовка задания (Жирный, Желтый цвет)
    std::cout << "\033[1;33m========== ЗАДАНИЕ " << task_number << " ==========\033[0m\n\n";

    // Описание (Белый цвет)
    std::cout << "Условие:\n\033[0m" << task_description << "\n\n";

    // Вывод формулы (Жирный, Бирюзовый цвет)
    std::cout << "\033[1;36m----------------------------------------\033[0m\n";
    std::cout << "\033[1;36mФОРМУЛА:\n" << formula << "\033[0m\n";
    std::cout << "\033[1;36m----------------------------------------\033[0m\n\n";

    // Инструкция для пользователя (Серый цвет)
    std::cout << "\033[90mВведите требуемые значения:\033[0m\n";
}


int main(int argc, char** argv) {
    // Пример вызова:
    display_task(
        1,
        "Необходимо вычислить значение функции Y, используя входные параметры A, B и X.",
        "Y = (A * X^2 + B) / SQRT(X) - C" // Адаптированная формула
    );

    // Здесь будет ваш код для ввода данных
    // double a, b, x, c;
    // std::cin >> a >> b >> x >> c;
    
    // ...
    return 0;
}
